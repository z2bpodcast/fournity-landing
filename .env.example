# Copy this file to .env.local for local development,
# and add the same variables in Vercel → Project → Settings → Environment Variables

# Supabase — find these in Supabase Dashboard → Project Settings → API
NEXT_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-public-key

# Yoco — your live publishable key
NEXT_PUBLIC_YOCO_PUBLIC_KEY=pk_live_6659251fdV6b2GJaeec4

# Where "Read Free Chapters" sends visitors (the digital book/flipbook URL)
NEXT_PUBLIC_BOOK_URL=https://fournity-digital.vercel.app
